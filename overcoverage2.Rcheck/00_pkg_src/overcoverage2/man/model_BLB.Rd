% Generated by roxygen2: do not edit by hand
\name{model_BLB}
\alias{model_BLB}
\title{Run the BLB model}
\usage{
model_BLB(part = 20, y = NULL, covariates = NULL, age = NULL, tin = NULL,
  combins = NULL, init_params = NULL, init = c(1, 0, 0, 0, 0, 0,
  0, 0), N = NULL, L = 8, num_bootstraps = 100, boot_start = 1,
  threads = 8, progress_path = NULL, results_path = NULL,
  cpp_file = NULL)
}
\arguments{
\item{part}{Partition index used for file naming when reading or saving.}

\item{y}{Observation matrix (individuals x time).}

\item{covariates}{Covariate matrix (individuals x 11).}

\item{age}{Age indicator array (individuals x 3 x time).}

\item{tin}{Time-in indicator array (individuals x 2 x time).}

\item{combins}{Register combination matrix.}

\item{init_params}{Initial parameter vector.}

\item{init}{Initial state distribution.}

\item{N}{Population size. Defaults to nrow(y).}

\item{L}{Number of registers.}

\item{num_bootstraps}{Number of bootstrap runs.}

\item{boot_start}{First bootstrap index to run.}

\item{threads}{Number of threads for RcppParallel.}

\item{progress_path}{Optional path for saving incremental results.}

\item{results_path}{Optional path for saving final estimates.}

\item{cpp_file}{Optional path to a C++ file to source via Rcpp::sourceCpp.}

}
\value{
A list with elements \code{estimates}, \code{means}, \code{sds},
and \code{results}.
}
\description{
Runs the BLB log-likelihood optimization loop for the model described in the
paper. For confidentiality, any data reads are commented out; inputs must be
provided directly.
}
\examples{
# See tests/model_BLB_simulated.R for a simulated run
}
